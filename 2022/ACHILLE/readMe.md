# pour filtrer le fichier json par défault

il suffit d'utiliser le fichier static/json/filterjson.py : python filterjson.py

# modifier le fichier à filtrer

ligne 3 : `with open("fly.json") as jsonFile:`
